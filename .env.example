# ============================================
# Next.js Template - Environment Variables
# ============================================
# Copy this file to .env.local and update with your values
# cp .env.example .env.local

# ============================================
# Application Configuration
# ============================================

# Application Environment
# Options: development, production, test
# Default: development
NODE_ENV=development

# ============================================
# Public Environment Variables
# ============================================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# ⚠️ Never put sensitive data in NEXT_PUBLIC_ variables

# Public Application URL
# Used for API calls and absolute URLs
# Format: http://localhost:3000 or https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# API Configuration (Optional)
# ============================================

# API Base URL (if different from NEXT_PUBLIC_APP_URL)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# ============================================
# Additional Notes
# ============================================
# 
# 1. All variables are optional except NODE_ENV (defaults to development)
# 2. Variables prefixed with NEXT_PUBLIC_ are accessible in the browser
# 3. Server-only variables (without NEXT_PUBLIC_) are only available in:
#    - API routes (app/api/)
#    - Server Components
#    - Server Actions
# 4. For production, always set:
#    - NODE_ENV=production
#    - NEXT_PUBLIC_APP_URL to your production URL
# 5. Never commit your .env.local file to version control
# 6. .env.local takes precedence over .env
# 7. Environment variables are validated at build time using Zod (see lib/env.ts)